# SafeTelecom Radio Console

if starting from npm, you must rename .env.sample to .env and fill in the variables

if starting from pm2, you must rename startup.json.sample to startup.json and do the same